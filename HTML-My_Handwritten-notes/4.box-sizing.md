### Box Sizing


box-sizing defines how an element’s width and height are calculated.

In content-box (default), width excludes padding and border, so the final size becomes larger.

In border-box, width includes content, padding, and border, making the element’s size predictable.
Modern layouts commonly use border-box for easier responsive design.